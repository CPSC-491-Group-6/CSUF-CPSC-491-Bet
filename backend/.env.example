PORT=3000
DB_PATH=./data/bet.db
NODE_ENV=development
## Generate a session secret with `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` and replace
SESSION_SECRET=replace-with-a-random-session-secret

